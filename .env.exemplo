# Remover o .exemplo e adicionar os valores

CLIENT_ID=?
# CLIENT_SECRET: usado apenas no fluxo HTTP Basic (obrigatório para o Sicredi)
CLIENT_SECRET=?
# Certificado no formato PEM (cert + chave privada)
CERT=?
PVK=?
# OU certificado no formato PKCS#12 (.pfx/.p12) + senha
CERT_PFX=?
PWD_PFX=?
# Token fixo usado quando sandbox_mode=True
SANDBOX_TOKEN=?
